# API Media Loaders

This compiled sample shows the application-level composition boundary: the
application supplies an `IApiClient`, while this package supplies typed texture,
text, and byte loaders.
